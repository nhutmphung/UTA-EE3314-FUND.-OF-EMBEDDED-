makes the led toggle on 5 times and off 5 times in 1 second, 10 Hz total. 

go to core/src/main.c for the main file, downlaod everything if you want to run it so that it works. all the files inside the src folder works together in unison
